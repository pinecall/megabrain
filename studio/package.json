{
  "name": "megabrain-studio",
  "private": true,
  "version": "1.0.0",
  "description": "The megabrain studio: a UI over the engine's own HTTP contracts.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && node build.mjs",
    "check": "npm run build && git diff --exit-code ../src/megabrain/transports/http/ui"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
