"""Deterministic passes over a finished answer. No model, ever.

Each one earns its place by what a measured reader went looking for and paid a
call to get: the definition of a helper the prose named, the test that pins the
behaviour described, the sections that ended up empty."""

from __future__ import annotations
