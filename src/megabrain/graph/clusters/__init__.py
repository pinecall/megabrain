"""Communities of files, and what to call them.

The package's ONLY touch of an LLM (`labels.py` + `_naming.py`), which is exactly
why it is a folder — hard rule #1 is about knowing where the model calls are, and
"somewhere in twenty-four files" is not knowing."""

from __future__ import annotations
