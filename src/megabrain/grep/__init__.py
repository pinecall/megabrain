"""WHERE TO EDIT — the lanes behind `megabrain_grep`, and NO model.

This is a different deliverable from the narration around it, and it arrived
after the package was laid out: `_mentions.py` sorted between `_litter.py`
(citation cleanup) and `_missing.py` (a narrator round) with nothing to say they
belong to different jobs.

Nothing here calls a model. The rows come from the index in ~50 ms, which is what
lets `grep` stand in for a literal search at all — and the reason this may
eventually leave `ask/` entirely, so the no-LLM invariant can cover it by test
rather than by convention."""

from __future__ import annotations
