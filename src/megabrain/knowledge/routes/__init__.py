"""Paths through the graph: how one file reaches another, and the toll.

A route is a QUESTION asked of the graph at query time, never a stored answer —
the graph supplies candidates and annotations, and never ranks (hard rule #3)."""

from __future__ import annotations
