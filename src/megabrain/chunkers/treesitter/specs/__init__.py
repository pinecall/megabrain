"""The language tables.

Every entry was earned by a real repository misreading itself: Ruby's `class <<
self` (sinatra's whole route DSL lived there, unnamed), Rust's `impl` blocks (no
`name` field at all), PHP's `const` (one level down), TypeScript's `export`
wrappers (which hide every declaration in a modern file), and a mocha `it(…)`
(which the grammar calls an expression statement)."""

from __future__ import annotations
